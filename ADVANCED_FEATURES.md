# 🚀 Fantasy Basketball Analyzer - 進階功能

## 新增的 4 大功能

基於你朋友的 Google Sheets 系統，我們加入了更強大的功能！

---

## 1️⃣ Google Sheets 自動同步 📊

**功能說明：**
像你朋友一樣，自動將數據同步到 Google Sheets，但加入更多智能分析！

**檔案位置：** `src/integrations/google_sheets_sync.py`

### 功能特色：
- ✅ 自動上傳陣容數據
- ✅ 同步聯盟排名
- ✅ 更新對戰結果
- ✅ Z-Score 球員排名
- ✅ 自動格式化和顏色標記

### 使用方式：

#### 步驟 1: 設定 Google Service Account

1. 前往 [Google Cloud Console](https://console.cloud.google.com/)
2. 建立新專案或選擇現有專案
3. 啟用 API:
   - Google Sheets API
   - Google Drive API
4. 建立 Service Account:
   - IAM & Admin → Service Accounts
   - Create Service Account
   - 下載 JSON 金鑰檔
5. 將 JSON 檔案儲存為 `config/google_credentials.json`

#### 步驟 2: 分享 Google Sheets

1. 建立新的 Google Sheets 或使用現有的
2. 點擊「共用」
3. 將 Service Account 的 email 加入 (在 JSON 檔案中的 `client_email`)
4. 給予「編輯者」權限

#### 步驟 3: 使用程式碼

```python
from src.integrations.google_sheets_sync import GoogleSheetsSync

# 初始化
sync = GoogleSheetsSync(
    credentials_file="config/google_credentials.json",
    spreadsheet_id="YOUR_SPREADSHEET_ID"  # 從網址取得
)

# 認證
sync.authenticate()
sync.connect_spreadsheet()

# 更新陣容
sync.update_roster_sheet(my_roster_data)

# 更新對戰
sync.update_matchup_sheet(matchup_data)

# 建立完整儀表板
sync.create_dashboard()
```

### 自動化：

```bash
# 每天自動同步
python3 auto_sync_to_sheets.py
```

---

## 2️⃣ 每週對戰預測引擎 ⚔️

**功能說明：**
AI 預測下週對戰的勝率，並給出策略建議

**檔案位置：** `src/analysis/matchup_predictor.py`

### 功能特色：
- ✅ 逐類別預測勝負
- ✅ 計算信心程度 (High/Medium/Low)
- ✅ 智能策略建議
- ✅ 識別可爭取的類別

### 使用方式：

```python
from src.analysis.matchup_predictor import MatchupPredictor

# 初始化
predictor = MatchupPredictor()

# 預測對戰
prediction = predictor.predict_matchup(
    my_roster=my_roster,
    opponent_roster=opponent_roster
)

# 生成報告
report = predictor.generate_matchup_report(prediction)
print(report)
```

### 輸出範例：

```
======================================================================
 週對戰預測報告
======================================================================

預測結果: Win
預計贏: 6 個類別
預計輸: 3 個類別
預計平: 0 個類別
勝率: 66.7%

📊 類別詳細分析
──────────────────────────────────────────────────────────────────────
✅ FG%: 0.492 vs 0.465 (High) - 穩贏
✅ FT%: 0.749 vs 0.720 (Medium) - 小贏
✅ 3PM: 1029 vs 850 (High) - 大贏
❌ REB: 3597 vs 4200 (High) - 大輸
⚖️ BLK: 422 vs 410 (Low) - 拉鋸

🎯 策略建議
──────────────────────────────────────────────────────────────────────
🔴 [Focus] 接近的類別需要加強: BLK, AST
   💡 這些類別還有機會爭取，注意球員的先發陣容調整

🟡 [Maintain] 穩贏的類別: FG%, 3PM, ST
   💡 保持現狀，確保主力球員健康上場

🔴 [Overall] 預測可以贏 6/9 個類別
   💡 ✅ 勝算很大！保持陣容穩定即可
```

---

## 3️⃣ 交易目標推薦系統 💼

**功能說明：**
基於你的陣容弱點，AI 智能推薦應該交易的目標球員

**檔案位置：** `src/analysis/trade_targets.py`

### 功能特色：
- ✅ 自動偵測陣容弱項
- ✅ 推薦補強目標 (Top 20)
- ✅ 識別可交易球員
- ✅ 建議完整交易包裹

### 使用方式：

```python
from src.analysis.trade_targets import TradeTargetRecommender

# 初始化
recommender = TradeTargetRecommender(
    my_roster=my_roster,
    available_players=all_league_players
)

# 推薦交易目標
targets = recommender.recommend_targets(
    target_categories=['FG%', 'REB', 'BLK'],  # 或自動偵測
    max_results=10
)

# 建議交易包裹
packages = recommender.suggest_trade_packages()

# 生成完整報告
report = recommender.generate_trade_report()
print(report)
```

### 輸出範例：

```
======================================================================
 交易目標推薦報告
======================================================================

📊 陣容分析
──────────────────────────────────────────────────────────────────────
優勢類別: 3PM, FT%, ST, A/T, PTS
劣勢類別: FG%, REB, DD

🎯 推薦補強 FG%, REB, DD 的球員
──────────────────────────────────────────────────────────────────────

#1 Domantas Sabonis (SAC)
   目標分數: 4.85
   總價值: 7.23
   強項: FG%, REB, AST, DD

#2 Jarrett Allen (CLE)
   目標分數: 4.12
   總價值: 5.89
   強項: FG%, REB, BLK, DD

#3 Walker Kessler (UTA)
   目標分數: 3.95
   總價值: 4.67
   強項: FG%, REB, BLK

📤 可以交易出去的球員
──────────────────────────────────────────────────────────────────────
  • Lonzo Ball (.385 FG% 拖累陣容)
  • Jayson Tatum (整季報銷)
  • Jalen Suggs (低效率，但你的優勢類別不需要)

💼 交易包裹建議
──────────────────────────────────────────────────────────────────────

送出: Lonzo Ball + Jalen Suggs
原因: 兩人 FG% 太低，送出後可以大幅改善 FG%
換來建議:
  • Domantas Sabonis - 能補強 FG%, REB, DD，完美契合需求
  • Jarrett Allen - 高效中鋒，籃板和火鍋都強
```

---

## 4️⃣ 自動化週報告生成器 📋

**功能說明：**
每週自動生成完整報告，整合所有分析結果

**檔案位置：** `src/automation/weekly_report.py`

### 功能特色：
- ✅ 完整陣容分析
- ✅ 對戰預測
- ✅ 交易建議
- ✅ 行動清單
- ✅ 自動儲存 (JSON + TXT)
- ✅ 可選 Google Sheets 同步

### 使用方式：

```python
from src.automation.weekly_report import WeeklyReportGenerator

# 初始化
generator = WeeklyReportGenerator(
    my_roster=my_roster,
    league_players=all_players
)

# 生成報告
report = generator.generate_full_report(
    opponent_roster=opponent_roster  # 可選
)

# 儲存報告
generator.save_report(report)
```

### 自動化設定：

#### 方法 1: crontab (macOS/Linux)

```bash
# 編輯 crontab
crontab -e

# 加入以下行 (每週一早上 8 點執行)
0 8 * * 1 cd /Users/murs/Documents/fantasy-basketball-analyzer && python3 generate_weekly_report.py
```

#### 方法 2: schedule 套件

```python
import schedule
import time

def job():
    # 執行週報告
    python3 generate_weekly_report.py

# 每週一早上 8 點
schedule.every().monday.at("08:00").do(job)

while True:
    schedule.run_pending()
    time.sleep(60)
```

#### 方法 3: 手動執行

```bash
python3 generate_weekly_report.py
```

### 輸出範例：

```
╔════================================================================╗
║                    Fantasy Basketball 週報告                       ║
╚════================================================================╝

生成時間: 2025-10-22 14:30:00
週次: Week 1

🏀 陣容狀態
──────────────────────────────────────────────────────────────────────
健康球員: 10 人
傷兵: 2 人
優勢類別: 3PM, FT%, ST, A/T, PTS
劣勢類別: FG%, REB, DD

⚔️  對戰預測
──────────────────────────────────────────────────────────────────────
預測結果: Win
勝率: 66.7%
預計贏: 6 個類別
預計輸: 3 個類別

💼 交易建議
──────────────────────────────────────────────────────────────────────
建議補強: FG%, REB, DD
推薦目標:
  1. Domantas Sabonis (SAC) - 分數: 4.85
  2. Jarrett Allen (CLE) - 分數: 4.12
  3. Walker Kessler (UTA) - 分數: 3.95

📋 本週行動清單
──────────────────────────────────────────────────────────────────────
🔴 [陣容改善] FG% 嚴重偏低
   💡 交易掉 Lonzo Ball，換取高 FG% 中鋒

🟡 [交易機會] 考慮交易獲取 Domantas Sabonis
   💡 補強 FG%, REB, DD

🟡 [傷病管理] 目前有 2 名傷兵
   💡 關注傷兵復出時間，考慮 IL 名單調整

════════════════════════════════════════════════════════════════════
祝你本週好運！🍀
════════════════════════════════════════════════════════════════════
```

---

## 🔥 綜合使用流程

### 每週例行工作流程：

#### 週一早上 (對戰開始前)
```bash
# 1. 生成週報告
python3 generate_weekly_report.py

# 2. 查看報告
cat data/reports/report_week_1_20251022.txt

# 3. 同步到 Google Sheets (可選)
python3 sync_to_sheets.py
```

#### 週三 (Mid-week check)
```bash
# 檢查對戰進度
python3 check_matchup_progress.py
```

#### 週五 (交易截止前)
```bash
# 評估交易機會
python3 analyze_trade_opportunities.py
```

#### 週日 (對戰結束)
```bash
# 查看結果和下週準備
python3 post_matchup_analysis.py
```

---

## 📦 完整安裝指南

### 1. 安裝進階套件

```bash
pip install -r requirements_advanced.txt
```

### 2. 設定 Google Sheets (可選)

按照上面「功能 1」的說明設定 Service Account

### 3. 設定自動化排程

選擇 crontab 或 schedule 方式

---

## 🆚 vs 你朋友的系統

| 功能 | 你朋友的系統 | 我們的系統 |
|------|-------------|-----------|
| **即時數據同步** | ✅ | ✅ |
| **Z-Score 排名** | ✅ | ✅ |
| **對戰詳情** | ✅ | ✅ |
| **對戰預測** | ❌ | ✅ NEW! |
| **交易建議** | ❌ | ✅ NEW! |
| **Punt 策略分析** | ❌ | ✅ NEW! |
| **自動化報告** | ❌ | ✅ NEW! |
| **行動清單** | ❌ | ✅ NEW! |
| **信心評級** | ❌ | ✅ NEW! |

---

## 🎯 下一步

你現在已經有：

1. ✅ **基礎系統** - Yahoo API + 陣容分析
2. ✅ **進階功能** - 預測 + 推薦 + 自動化
3. ✅ **整合工具** - Google Sheets 同步

**建議立即執行：**

```bash
# 測試週報告生成
python3 generate_weekly_report.py

# 測試 Google Sheets 同步 (需先設定認證)
python3 test_sheets_sync.py
```

**長期使用：**
- 設定 crontab 每週一自動生成報告
- 每週查看報告做決策
- 根據建議執行交易

祝你奪冠！🏆🏀
