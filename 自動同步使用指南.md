# Fantasy Basketball 自動同步使用指南 ⚡

## 🎉 自動同步已設定完成！

你的系統現在會**每小時自動**將 Yahoo Fantasy 數據同步到 Google Sheets！

---

## ⏰ 自動同步時間表

---

## ⚠️ 重要：macOS 權限設定

**這是最常見的問題！** 由於 macOS 的安全機制，你必須手動授權 `cron` 服務存取你的檔案，否則自動同步會失敗（日誌顯示 `Operation not permitted`）。

### 如何授權 Full Disk Access：

1.  打開 **「系統設定」(System Settings)**
2.  進入 **「隱私權與安全性」(Privacy & Security)**
3.  點擊 **「完全取用磁碟」(Full Disk Access)**
4.  點擊列表底部的 **「+」** 號（可能需要輸入密碼）
5.  按下 **`Command` + `Shift` + `G`**，輸入 `/usr/sbin/cron` 並按 Enter
6.  選取 `cron` 並點擊 **「打開」(Open)**
7.  確保 `cron` 旁邊的開關是 **開啟 (藍色)** 的

---

## ⏰ 自動同步時間表

**目前設定：每小時的第 0 分鐘執行**

執行時間：
- 00:00 (凌晨 12 點)
- 01:00 (凌晨 1 點)
- 02:00 (凌晨 2 點)
- ... 每小時一次
- 23:00 (晚上 11 點)

**下次同步時間：** 下一個整點

---

## 📊 查看同步結果

### 方法 1：查看 Google Sheets
直接打開：https://docs.google.com/spreadsheets/d/19EPLNaYJTHlqZbMLlaQgF-OtmjLDMmb46ldem1380-o

### 方法 2：查看同步日誌
```bash
# 查看最近 20 行日誌
tail -20 ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log

# 實時監控日誌
tail -f ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log

# 查看所有日誌
cat ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log
```

---

## 🎯 手動同步（隨時可用）

如果你想立即同步，不用等到整點：

### 方法 1：使用快速同步腳本
```bash
cd ~/Documents/fantasy-basketball-analyzer
./quick_sync.sh
```

### 方法 2：使用自動同步腳本
```bash
cd ~/Documents/fantasy-basketball-analyzer
./auto_sync.sh
```

### 方法 3：直接執行同步
```bash
cd ~/Documents/fantasy-basketball-analyzer
python3 sync_to_sheets.py
```

---

## ⚙️ 修改同步頻率

### 查看目前設定
```bash
crontab -l
```

### 修改設定
```bash
crontab -e
```

### 常用設定範例

**每 2 小時同步一次：**
```
0 */2 * * * /Users/murs/Documents/fantasy-basketball-analyzer/auto_sync.sh
```

**每天早上 8 點同步：**
```
0 8 * * * /Users/murs/Documents/fantasy-basketball-analyzer/auto_sync.sh
```

**每天早上 8 點和晚上 8 點同步：**
```
0 8,20 * * * /Users/murs/Documents/fantasy-basketball-analyzer/auto_sync.sh
```

**每 30 分鐘同步一次：**
```
*/30 * * * * /Users/murs/Documents/fantasy-basketball-analyzer/auto_sync.sh
```

**工作日（週一到週五）每小時同步：**
```
0 * * * 1-5 /Users/murs/Documents/fantasy-basketball-analyzer/auto_sync.sh
```

---

## 🔍 檢查自動同步狀態

### 確認 crontab 已設定
```bash
crontab -l
```
應該看到：
```
0 * * * * /Users/murs/Documents/fantasy-basketball-analyzer/auto_sync.sh
```

### 檢查最近一次同步
```bash
tail -5 ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log
```

### 檢查今天的所有同步記錄
```bash
grep "$(date '+%Y-%m-%d')" ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log
```

---

## 🛠️ 故障排除

### 問題 1：同步沒有執行或顯示 "Operation not permitted"

**症狀：**
- `logs/cron.log` 出現 `/bin/bash: ...: Operation not permitted`
- 或者完全沒有日誌

**解決方法：**
請參考上方的 **「⚠️ 重要：macOS 權限設定」** 章節，給予 `cron` 完全取用磁碟權限。這是 macOS 的強制安全要求。

**檢查 crontab：**

**檢查 crontab：**
```bash
crontab -l
```

**檢查腳本權限：**
```bash
ls -la ~/Documents/fantasy-basketball-analyzer/auto_sync.sh
```
應該顯示 `-rwxr-xr-x`（有執行權限）

**手動測試腳本：**
```bash
~/Documents/fantasy-basketball-analyzer/auto_sync.sh
```

### 問題 2：同步失敗

**查看錯誤日誌：**
```bash
tail -50 ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log | grep "❌"
```

**手動測試同步：**
```bash
cd ~/Documents/fantasy-basketball-analyzer
python3 sync_to_sheets.py
```

### 問題 3：日誌檔案太大

日誌會自動保持在 1000 行以內，但如果需要清空：
```bash
> ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log
```

---

## 🎮 管理自動同步

### 暫停自動同步
```bash
# 移除 crontab（保留備份）
crontab -l > ~/crontab_backup.txt
crontab -r
```

### 恢復自動同步
```bash
# 從備份恢復
crontab ~/crontab_backup.txt

# 或重新設定
echo "0 * * * * /Users/murs/Documents/fantasy-basketball-analyzer/auto_sync.sh" | crontab -
```

### 完全停止自動同步
```bash
crontab -r
```

---

## 📱 監控建議

### 每日檢查（推薦）
每天查看一次日誌，確保同步正常：
```bash
tail -20 ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log
```

### 每週檢查
確認 Google Sheets 數據是最新的：
https://docs.google.com/spreadsheets/d/19EPLNaYJTHlqZbMLlaQgF-OtmjLDMmb46ldem1380-o

---

## 🎊 完成！

你的 Fantasy Basketball 系統現在是**全自動**的了！

✅ **每小時自動同步**
✅ **自動記錄日誌**
✅ **隨時手動同步**
✅ **Google Sheets 實時更新**

---

## 📞 快速指令參考

```bash
# 查看自動同步設定
crontab -l

# 查看最近的同步日誌
tail -20 ~/Documents/fantasy-basketball-analyzer/logs/auto_sync.log

# 立即手動同步
~/Documents/fantasy-basketball-analyzer/auto_sync.sh

# 查看 Google Sheets
open https://docs.google.com/spreadsheets/d/19EPLNaYJTHlqZbMLlaQgF-OtmjLDMmb46ldem1380-o

# 查看陣容狀態
cd ~/Documents/fantasy-basketball-analyzer && python3 my_weekly_report.py
```

---

**享受你的全自動 Fantasy Basketball 智庫系統！** 🏀🤖
